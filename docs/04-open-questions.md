# 4. Ответы и оставшиеся вопросы

Дата последних ответов: 11.09.2026.

## 4.1 Решено

### R1. Ручные данные

Все данные ниже вводятся вручную в Excel. Источники разные и вне файла 1С.

| Поле | Ячейка | Пример |
| --- | --- | --- |
| Неучтёнка | `B1` | `Неучтёнка 224,00` |
| Контрольное число | `C1` | `5299?` |
| Дата | `B3` | `17.08.2026` |
| Признак инвентаризации | `C5` | `Планово` |
| Итог с неучтёнкой | `I5` | `=I4+224` |
| Комментарий | `L` строки данных | `неучтёнка` |
| Строки товаров без кода | любое место | вставка вручную |
| Блок сотрудников | подвал, `B` и `C` | ФИО и число |

Метка `не+`, обнуление `J`, цветные заливки и рамки ставятся автоматически (см. R8). Человек только проверяет результат.

### R2. Среда работы

Веб-приложение на Python. Сервер: Ubuntu Server. Подробности: `05-architecture.md`.

### R3. Граница работы сайта

1. Сайт делает преобразование файла и показывает отчёты. Форм для ручных полей нет.
2. Список сотрудников в базе не хранится.
3. Входа по паролю нет.
4. История файлов не хранится.
5. База данных не нужна.

### R4. Доступ

1. Служба слушает только `127.0.0.1:8000`. Порт свободен.
2. Домена нет, HTTPS и Nginx не нужны.
3. Доступ через SSH-туннель: `http://localhost:8000`.

### R5. Формат даты

В `B3` записывается дата в формате `ДД.ММ.ГГГГ` (`dd.mm.yyyy`). Формат `mm-dd-yy` из первой версии документации отменён 11.09.2026.

### R6. Подсветка расхождений

Заливка — постоянный стиль ячейки, без условного форматирования.

### R7. Строки данных

1. Порядок строк из 1С сохраняется полностью.
2. Пустые строки в группы не добавляются.

### R8. Автоматические пересорты

1. Поиск только внутри группы товаров.
2. Бренд — совпадение первых слов имени. Учитываются исключения: кириллица и латиница, мелкие вариации записи.
3. Вкусы и другие варианты не разделяются. Пересорт считается по всему бренду сразу.
4. Размер расхождения не важен. Цена не проверяется совсем, столбец `G` справочный.
5. Если итог количества по бренду положительный или нулевой — это пересорт: рамка вокруг `F`, `J = 0`, заливки нет.
6. Излишек никогда не считается в плюс. Положительная сумма обнуляется.
7. Чистый излишек без минусов: оранжевая заливка, метка `не+`, `J = 0`.
8. Недостача: жёлтая заливка, в `J` остаётся значение из 1С без пересчёта.
9. Рамка пересорта: средняя чёрная.

Полное правило: `06-resort-rules.md`.

### R9. Отчёты на странице

1. Таблица по группам товаров: пересорты в штуках и в сумме, недостача, число строк без пары.
2. Таблица состава гроздей: строки, имена, `F`, `J`, решение программы.
3. Таблица сомнительных совпадений имён с мерой схожести от 0,70 до 0,90.
4. В файл отчёты не записываются.

### R10. Ремонт файла из 1С

Основной способ — вставка пустого `xl/sharedStrings.xml` в архив. Стили и геометрия из 1С сохраняются. Конвертация через LibreOffice только запасная, включается настройкой `REPAIR_MODE`.

### R11. Имя склада

Берётся из имени входного файла (часть до первого пробела или до слова `без`). В шапке 1С значение склада пустое. Настройка: `WAREHOUSE_SOURCE`.

### R12. Диапазон формулы итога группы

`=SUM(J<первая строка данных>:J<строка перед итогом>)`. Пустые строки между данными и итогом входят в диапазон.

### R13. Столбец `L`

В готовом файле столбец рабочий, ширина 55,83, в нём комментарии. Входная ширина 0,5 не сохраняется.

### R14. Автофильтр

Ставится на столбец `K` от строки 1 до последней строки, как в образце.

### R15. Счёт «Пересорты, шт.»

Сумма модулей `F` по разобранным строкам, делённая на 2. Так одна пара `-1`/`+1` даёт 1 штуку.

## 4.2 Открытых вопросов нет

Все вопросы первичного разбора закрыты. Можно писать код.

## 4.3 Вопросы будущих версий

1. Автоматический источник для полей `B1`, `C1`, `B3`, `C5`.
2. Шаблон блока сотрудников по складам.
3. Словарь брендов с ручными исключениями.
4. Проверка имени склада по списку складов, если имена файлов окажутся нестабильными.
